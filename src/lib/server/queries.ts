// consider if this is needed or not

// import pg from 'pg';
// import { db } from './db';

// class dbConnection {
// 	private db: pg.Client;

// 	constructor(db: pg.Client) {
// 		this.db = db;
// 	}
// }

// export const dbQuery = new dbConnection(db);
